/* const getCard = require('../../src/pages');

function renderPost(post) {

    document.querySelector('.card-blog__read-more').addEventListener('click', async e => {
        const postData = await getCard(post);
        document.querySelector('.header__title').innerHTML = `${postData.title}`;
        document.querySelector('.cards-blog').innerHTML = '';
        document.querySelector('.pagination').innerHTML = '';
    })

}


module.exports =  {
    renderPost
} */